import { Context } from '../types/bottender';

export const withdraw = async (context: Context) => {
  // TODO get user address
  // TODO withdraw to address
  await context.sendText('Ehehehe no way!!');
};
